console.log(21);
// first line
console.log('It was love at first sight.');
/*
console.log('The first time i saw you, i fell madly in love with you.');
*/
console.log('JavaScript');
console.log(20.49);
console.log(21+3.5);
console.log(2026-1969);
console.log(0.2708*100);
console.log(65/240);
console.log(11 % 3);
console.log('Hello '+'He');
console.log('Teaching myself how to code'.length);
console.log('Codecademy'.toUpperCase());
console.log('    Remove whitespace   '.trim());
console.log('Hey'.startsWith('H'));
console.log(Math.floor(Math.random() * 50));

/*
- Data is printed, or logged, to the console, a panel that displays messages, with console.log().
- We can write single-line comments with // and multi-line comments between /* and /.

There are eight fundamental data types in JavaScript: numbers, BigInts, strings, booleans, null, undefined, symbols, and objects.
- Numbers are any number without quotes. For example: 23.8879
- Strings are characters wrapped in single or double quotes. For example: 'Sample String'
- The built-in arithmetic operators include +, -, *, /, and %.
- Objects, including instances of data types, can have properties that store information.
  The properties are denoted with a . after the name of the object, for example: 'Hello'.length.
- Objects, including instances of data types, can have methods that perform actions.
  Methods are called by appending the object or instance with a period, the method name, and parentheses.
    For example: 'hello'.toUpperCase().
- We can access properties and methods by using the . (dot operator).
- Built-in objects, including Math, are collections of methods and properties that JavaScript provides.
*/
